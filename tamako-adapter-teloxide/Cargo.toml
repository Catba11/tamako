[package]
name = "tamako-adapter-teloxide"
version = "0.1.0"
edition = "2021"

[dependencies]
tamako-core.workspace = true
tamako-store.workspace = true
tamako-vision.workspace = true
teloxide.workspace = true
time.workspace = true
tokio.workspace = true
tracing.workspace = true

# StreamExt for the polling listener stream (teloxide 0.17 exposes the
# listener as a futures Stream; there is no owned-stream alternative).
futures = "0.3"

[dev-dependencies]
serde_json.workspace = true

# Tempdir MediaStore for the media enrichment tests (media.rs).
tempfile = "3"
